export class Frontend {
    public frontendId: number;
    public description: string;

    constructor(frontendId: number, description: string) {
        this.frontendId = frontendId;
        this.description = description;
    }

}